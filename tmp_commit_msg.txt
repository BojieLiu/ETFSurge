cd "E:\ETF_Surge"
git commit -m "feat: implement 5 quality improvements from review report; update plan docs

P0 — Unify market regime detection across design and strategy-check
    paths by reusing pool_manager.get_market_regime() instead of
    self-fetching _detect_regime() in strategy_check().

P0 — Filter extreme drawdown (>40% monthly loss) ETFs from
    portfolios via filter_extreme_drawdown() in risk_controls.py.

P1 — Defense asset effectiveness check: halve weight of defense-layer
    holdings with >10% 3-month loss via check_defense_effectiveness().

P1 — Candidate pool freshness check: remove ETFs without any price or
    return data via remove_stale_candidates().

P2 — Rationale template diversity: introduce 5-phrase pools per layer
    with deterministic MD5-hash selection for natural language variety.

Docs: add docs/five-improvements-plan.md (design doc),
    docs/implementation-status.md (plan-vs-code audit);
    update engine/rationale.py, engine/risk_controls.py,
    portfolio_service.py with all improvements."
