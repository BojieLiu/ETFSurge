[pytest]
testpaths = tests
addopts = -m "not integration"
asyncio_mode = auto
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: requires live data sources (not isolated)
