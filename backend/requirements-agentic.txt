# Agentic 扩展依赖（v7 P1.5 LangGraph 对照实现）。
# 与主 requirements.txt 隔离——默认不装，仅做对照/面试用。
# 安装: pip install -r requirements-agentic.txt
# CI: 默认不跑 P1.5；pytest -m agentic 才跑 LangGraph 相关测试（v7 §7.5）。
mcp>=1.0,<2.0
langgraph>=1.2,<2.0
