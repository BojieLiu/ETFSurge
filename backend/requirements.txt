fastapi>=0.115.0
uvicorn[standard]>=0.30
akshare>=1.18.0
yfinance>=0.2.0
sqlalchemy>=2.0
aiosqlite>=0.20
redis>=5.0
apscheduler>=3.10
feedparser>=6.0
httpx>=0.27
pydantic-settings>=2.0
pytest>=8.0
pytest-timeout>=2.3
pytest-xdist>=3.0
tushare>=1.2.89
requests>=2.28
# round11 EM 根因路线 A: curl_cffi（浏览器 TLS 指纹）绕容器侧 EM 拦截；wheel 捆绑 libcurl，无系统依赖
curl_cffi>=0.15.0
levistock>=0.1.0
# R5-2-4: mootdx（通达信 TCP 行情，A 股实时/批量降级链第一环）改由 Dockerfile 以 --no-deps 安装。
# 原因：mootdx 0.11.7 依赖 httpx>=0.25,<0.26，与项目 httpx>=0.27 冲突，pip -r 直接装会 ResolutionImpossible；
# 本地/容器均验证 httpx 0.28.x 与 mootdx 0.11.7 运行时兼容（docker build 曾因该冲突失败，见 round6 诊断）。
# mootdx 0.11.7 内置 42 目标 16 可达，默认首个 110.41.147.114:7709 延迟 60ms；
# 注意：Docker python:3.14-slim 若 py-mini-racer 无 wheel 需固定版本或跳过（实测有 wheel，见 round5 附录 C）。
scipy>=1.14.0
# pandas_ta>=0.3.14b0  # 移除: numba 不支持 Python 3.14, Dockerfile 中用 --no-deps 安装
python-multipart>=0.0.9

# ──── Dev / CI tools ────────────────────────────────────────────
mypy>=2.0.0
pyinstrument>=5.0.0
