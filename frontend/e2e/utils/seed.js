// E2E test data injection — skeleton
// TODO: implement API-based seeding (e.g. POST /test/seed) and cleanup hooks.
// Example:
//   export async function seedTestData() {
//     await fetch('http://localhost:8000/test/seed', { method: 'POST', body: JSON.stringify({ ... }) })
//   }
//   export async function cleanupTestData() {
//     await fetch('http://localhost:8000/test/cleanup', { method: 'POST' })
//   }

export async function seedTestData() {
  // Placeholder — implement when a test-data API endpoint is available.
  console.warn('[seed] seedTestData not yet implemented')
}

export async function cleanupTestData() {
  // Placeholder — implement alongside seedTestData.
  console.warn('[seed] cleanupTestData not yet implemented')
}
